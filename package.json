{
  "name": "moonsea-codex-theme",
  "version": "1.1.0",
  "description": "通过本地助手即时切换 Codex 普通主题，并为 Pro 主题预留运行时能力",
  "type": "module",
  "scripts": {
    "build": "node ./scripts/build-tool.mjs",
    "test": "npm run build && node --test ./tests/*.test.mjs"
  },
  "engines": {
    "node": ">=22.12"
  },
  "devDependencies": {
    "@electron/asar": "^4.2.0",
    "esbuild": "^0.28.1"
  },
  "license": "MIT"
}

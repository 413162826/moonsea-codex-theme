{
  "name": "moonsea-codex-theme",
  "version": "1.2.0",
  "description": "通过本地助手即时切换 Codex 普通主题与 Pro 运行时主题",
  "type": "module",
  "scripts": {
    "build": "node ./scripts/build-tool.mjs",
    "test": "npm run build && node --test ./tests/*.test.mjs"
  },
  "engines": {
    "node": ">=22.12"
  },
  "devDependencies": {
    "@electron/asar": "^4.2.0",
    "esbuild": "^0.28.1"
  },
  "license": "MIT"
}

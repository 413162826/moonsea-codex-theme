{
  "name": "moonsea-codex-theme",
  "version": "1.5.2",
  "description": "通过本地助手即时切换 Codex 免费渐变壁纸与 Pro 精选壁纸",
  "type": "module",
  "scripts": {
    "wallpapers": "node ./scripts/sync-wallpapers.mjs",
    "build": "npm run wallpapers && node ./scripts/build-tool.mjs",
    "test": "npm run build && node --test ./tests/*.test.mjs"
  },
  "engines": {
    "node": ">=22.12"
  },
  "devDependencies": {
    "@electron/asar": "^4.2.0",
    "esbuild": "^0.28.1",
    "sharp": "^0.35.3"
  },
  "license": "MIT"
}

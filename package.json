{
  "name": "moonsea-codex-theme",
  "version": "1.0.0",
  "description": "为 Codex 桌面客户端生成独立的月海主题副本",
  "type": "module",
  "scripts": {
    "build": "node ./scripts/build-tool.mjs",
    "test": "npm run build && node --test ./tests/*.test.mjs"
  },
  "engines": {
    "node": ">=22.12"
  },
  "devDependencies": {
    "@electron/asar": "^4.2.0",
    "esbuild": "^0.28.1"
  },
  "license": "MIT"
}
